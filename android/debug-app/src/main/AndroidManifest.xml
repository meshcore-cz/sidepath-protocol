<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permissions, features and the SidepathService are declared in the :sidepath-networking library
         manifest and merged in. -->
    <application
        android:name="cz.meshcore.sidepath.debug.SidepathDebugApp"
        android:label="Sidepath Debug"
        android:theme="@style/Theme.Sidepath">
        <activity
            android:name="cz.meshcore.sidepath.debug.ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
